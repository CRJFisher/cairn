# Documentation refresh

Two prose steps. Neither states a checkable end state, and the plan offers no command.

## Steps

1. **Rewrite the getting-started guide** so a new reader can install and run the tool
   without opening any other page.
2. **Bring the architecture page into line with the current module layout**, after the
   getting-started rewrite settles the vocabulary.
