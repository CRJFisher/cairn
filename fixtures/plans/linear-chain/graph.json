{
  "cairn_graph_version": 2,
  "plan": {
    "slug": "linear-chain",
    "title": "Token bucket rate limiting",
    "source": "README.md",
    "sources": [
      {
        "path": "README.md",
        "sha256": "b8cc996342a72ed6dd0722057849cc6f286ceef196f32921cc74908728ea1508"
      }
    ],
    "default_kind": "agent.claude",
    "id_collisions": []
  },
  "steps": [
    {
      "id": "bucket_store",
      "slug": "1. Bucket store",
      "title": "Bucket store",
      "task": "Bring `src/limiter/store.py` to a state where it holds a `TokenBucket` dataclass and an in-memory store keyed by client id, with refill computed from elapsed time rather than a background timer.",
      "deps": [],
      "verify": "python3 -m pytest tests/test_store.py -q",
      "assertion": null,
      "tools": null,
      "scope": "once",
      "reads": [],
      "retries": 0,
      "kind": "agent.claude",
      "timeout": 3600,
      "max_budget_usd": 5.0,
      "model": "sonnet"
    },
    {
      "id": "middleware",
      "slug": "2. Middleware",
      "title": "Middleware",
      "task": "Bring `src/limiter/middleware.py` to a state where every inbound request consumes one token and a depleted bucket returns 429 with a `Retry-After` header.",
      "deps": [
        {
          "id": "bucket_store",
          "origin": "declared",
          "evidence": "depends on the bucket store"
        }
      ],
      "verify": "python3 -m pytest tests/test_middleware.py -q",
      "assertion": null,
      "tools": null,
      "scope": "once",
      "reads": [],
      "retries": 0,
      "kind": "agent.claude",
      "timeout": 3600,
      "max_budget_usd": 5.0,
      "model": "sonnet"
    },
    {
      "id": "gateway_wiring",
      "slug": "3. Gateway wiring",
      "title": "Gateway wiring",
      "task": "Bring `src/gateway/app.py` to a state where the middleware is installed ahead of routing and its limits come from `config/limits.toml`.",
      "deps": [
        {
          "id": "middleware",
          "origin": "declared",
          "evidence": "depends on the middleware"
        }
      ],
      "verify": "python3 -m pytest tests/test_gateway.py -q",
      "assertion": null,
      "tools": null,
      "scope": "once",
      "reads": [],
      "retries": 0,
      "kind": "agent.claude",
      "timeout": 3600,
      "max_budget_usd": 5.0,
      "model": "sonnet"
    }
  ],
  "omissions": [],
  "questions": []
}
