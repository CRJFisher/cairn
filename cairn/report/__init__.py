"""The run report: one record, three renderings, one order.

Not to be confused with `cairn/plan/report.py`, which is the parse report a plan's authoring
produces. This package renders a *run*, after the fact, from the model doc 12 froze.
"""
