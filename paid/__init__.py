"""The suite that spends money, kept where the free command cannot reach it.

Nothing here is imported by `cairn/`. The dependency runs one way — the suite reads the
package it measures, and the package knows nothing about the suite — so extracting Cairn to
a public repository carries this directory or leaves it without either decision touching the
other.
"""
